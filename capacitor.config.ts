import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front_proyecto_final_v2',
  webDir: 'www'
};

export default config;
